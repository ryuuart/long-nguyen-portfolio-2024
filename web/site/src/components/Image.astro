---
interface Props {
  src: string;
  alt?: string;
  width?: string;
}

const { src, alt = "", width = "10000" } = Astro.props;
---

<img decoding="async" src={src} alt={alt} width={width} />

<style>
  img {
    display: block;
  }
</style>
